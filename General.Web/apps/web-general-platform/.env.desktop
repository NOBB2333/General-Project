VITE_BASE=./

# 桌面端构建产物按本地文件加载，因此资源路径必须使用相对路径。

# 当前先指向本地后端默认开发地址，后续可改为客户端配置项。
VITE_GLOB_API_URL=https://localhost:44392

# 桌面端先沿用同一组 APP 范围。
VITE_GLOB_APP_CODES=platform

VITE_COMPRESS=none
VITE_PWA=false
VITE_ROUTER_HISTORY=hash
VITE_INJECT_APP_LOADING=true
VITE_ARCHIVER=false
